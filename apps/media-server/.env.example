# Public URL where media will be accessible (include protocol, no trailing slash)
PUBLIC_URL=https://media.yourdomain.com

# Optional API key for upload authentication (leave empty to disable)
API_KEY=your-secret-api-key-here

# Port for the API server
PORT=3001

# Upload directory (inside container, mapped via volume)
UPLOAD_DIR=/uploads
